name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  smoke:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x, 22.x]
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install dependencies (production)
        run: npm ci --omit=dev
        continue-on-error: true
      - name: Install dependencies (fallback)
        if: failure()
        run: npm install --omit=dev
      - name: Syntax check
        run: npm run smoke:syntax
      - name: Smoke audit
        run: npm run smoke:audit
      - name: Unit tests
        run: npm test
      - name: Integration tests
        run: npm run test:integration
      - name: Smoke boot (require + factory check)
        run: npm run smoke:boot
      - name: Check modules syntax (all split folders)
        run: |
          for f in modules/*.js lib/*.js db/*.js payment/*.js accounts/*.js admin/*.js scheduler/*.js scripts/*.js tests/integration/*.js; do
            if [ -f "$f" ]; then
              node --check "$f"
            fi
          done
