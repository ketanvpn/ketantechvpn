# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts harus LF supaya bisa dieksekusi di Linux VPS (CRLF bikin error "bad interpreter").
*.sh text eol=lf
update.sh text eol=lf
start text eol=lf
cek-port.sh text eol=lf
